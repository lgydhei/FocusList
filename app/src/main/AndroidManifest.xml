<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络相关 API：访问“每日一言” -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 通知（Android 13+ 需运行时授权） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 前台服务：番茄钟计时 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 多媒体：完成时振动 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 广播接收器：开机后重排任务提醒 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".FocusListApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusList"
        tools:targetApi="34">

        <!-- ============ Activity（四大组件之一：活动） ============ -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.addedit.AddEditTaskActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.template.TemplateActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.review.FocusReviewActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.review.ReviewHistoryActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ============ Service（四大组件之一：服务） ============ -->
        <!-- 前台服务运行番茄钟倒计时，退到后台仍持续；可循环播放白噪音 -->
        <service
            android:name=".service.FocusTimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="pomodoro_focus_timer" />
        </service>

        <!-- ============ BroadcastReceiver（四大组件之一：广播接收器） ============ -->
        <!-- 任务到点提醒：由 AlarmManager 触发 -->
        <receiver
            android:name=".receiver.ReminderReceiver"
            android:exported="false" />

        <!-- 开机广播：重启后重新排程所有未完成任务的提醒 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ============ ContentProvider（四大组件之一：内容提供器） ============ -->
        <!-- 对外/跨进程暴露任务数据，供桌面小组件读取 -->
        <provider
            android:name=".provider.TaskProvider"
            android:authorities="com.bistu.focuslist.provider"
            android:exported="false"
            android:grantUriPermissions="true" />

        <!-- 桌面小组件：通过 ContentProvider 读取今日待办数量 -->
        <receiver
            android:name=".widget.TaskWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.bistu.focuslist.ACTION_WIDGET_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/task_widget_info" />
        </receiver>

    </application>

</manifest>
